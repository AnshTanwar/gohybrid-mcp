# gohybrid-mcp does NOT use environment variables for credentials.
# All credentials arrive per-session via the Authorization: Bearer ghi_<token> header.
#
# Generate tokens at /connect (no server-side storage, nothing to configure here).
#
# The only optional variable is PORT (Railway injects this automatically):
# PORT=8000
